'use strict'

const generator = require('./generator')

// -------------------------------------------------------------
// Tests.
// -------------------------------------------------------------

describe('todo()', () => {
  it('should do…', () => {
    expect(generator).toBeDefined()
  })
})
